#include <stdio.h>

int main()
{
    int a = 50.45, b = 60, sum; // 50 assigned in a. because we defined the value of a is integer type.

    sum = a + b;

    printf("%d + %d = %d", b, a, sum); // first %d is for b. second for a. and third for sum..

    return 0;
}
