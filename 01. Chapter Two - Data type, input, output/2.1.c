#include <stdio.h>

int main()
{
    int a = 50, b = 60, sum; // 'a' and 'b' is a variable. 50 and 60 assigned on it.

    sum = a + b;

    printf("sum is %d", sum); // The value of sum will print in %d

    return 0;
}
