#include <stdio.h>

int main()
{
    int n = -1;

    if(n >= 0) {
        printf("The number is positive");
    }
    else {
        printf("The number is negetive");
    }

    return 0;
}

/* If The condition is  true, the statement of (if) will print.
If The condition is false, the statement of (else) will print. */
