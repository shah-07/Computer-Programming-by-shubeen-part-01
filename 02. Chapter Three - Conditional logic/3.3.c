#include <stdio.h>

int main()
{
    int n = -1;

    if(n > 0) {
        printf("the number is positive");
    }
    else if(n < 0) {
        printf("The number is negetive");
    }
    else {
        printf("The number is Zero\n");
    }

    return 0;
}
