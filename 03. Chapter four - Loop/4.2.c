#include <stdio.h>

int main()
{
    int n = 1;

    while(n <=10) { //Its functions inside the loop continue as long as the condition is true
        printf("%d\n", n); //print the value of n
        n++; //increase 1 the value of n
    }

    return 0;
}
